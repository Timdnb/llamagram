{
  "mcpServers": {
    "time": {
      "command": "uv",
      "args": [
        "run",
        "--with",
        "mcp",
        "python",
        "${ROOT_DIR}/mcps/time/server.py",
        "--local-timezone=${AGENT_MCP_LOCAL_TIMEZONE}"
      ]
    }
  }
}
